import SwiftUI

// TODO: generate it
extension Color {
    static let backgroundWarningLight = Color("backgroundWarningLight")
    
    static let candleRed = Color("candleRed")
    static let candleGreen = Color("candleGreen")
    
    static let textSecondary = Color("textSecondary")
    static let textSuccess = Color("textSuccess")
    static let textError = Color("textError")
    static let textPriceUp = Color("textPriceUp")
    static let textPriceDown = Color("textPriceDown")
    
    static let shimmerPrimaryFrom = Color("shimmerPrimaryFrom")
    static let shimmerPrimaryTo = Color("shimmerPrimaryTo")
}
